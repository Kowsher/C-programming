#include<stdio.h>
#include<string.h>

int main()
{
    char ch[1000];
    gets(ch);
    int i=0;

    while(ch[i])
    {
       if(isupper(ch[i]))putchar(tolower(ch[i]));
       else putchar(toupper(ch[i]));
        i++;
    }

    return 0;
}

