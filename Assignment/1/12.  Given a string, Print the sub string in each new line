#include<stdio.h>
#include<string.h>

int main()
{
    char ch[1000];
    gets(ch);
    int i=0;

    while(ch[i])
    {
       if(isspace(ch[i])) printf("\n");
       else putchar(ch[i]);
        i++;
    }

    return 0;
}

