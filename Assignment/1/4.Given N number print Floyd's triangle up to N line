#include<stdio.h>
void main()
{
	int n,a=1;
	scanf("%d",&n);
	for (int i = 1; i <= n; i++)
    {
        for (int c = 1; c <= i; c++)
        {
            printf("%d ",a);
            a++;
        }
        printf("\n");
  }

}
