#include<stdio.h>
#include<string.h>

int main()
{
    char ch[1000];
    gets(ch);
    int i=0;

    while(ch[i])
    {
       if(isspace(ch[i])) printf("%c ", ch[i+1]);
       if(i==0)printf("%c ", ch[i]);
        i++;
    }

    return 0;
}

