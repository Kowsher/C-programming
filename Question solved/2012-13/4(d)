#include<stdio.h>

int main ()
{
    int a,sumeven=0,sumodd=0;
    for(int i=0; i<4; i++)
    {
        scanf("%d", & a);
        if(a%2==0)sumeven+=a;
        else sumodd+=a;
    }
    printf("%d %d\n", sumeven, sumodd);

    return 0;
}
