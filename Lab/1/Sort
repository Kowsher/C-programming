#include<bits/stdc++.h>
using namespace std;


int main()
{
    int n, arr[100],i;
    scanf("%d", &n);
    for(i=0; i<n; i++)scanf("%d", &arr[i]);
    sort(arr, arr+n);
    for(i=0; i<n; i++)printf("%d ", arr[i]);

}
