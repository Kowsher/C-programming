#include<stdio.h>
#include<math.h>
int main(){

    FILE * fpointer;
    fpointer=fopen("kowsher.txt", "r");
    char str[100];
    fscanf(fpointer,"%s", str);
    printf("%s", str);
    fclose(fpointer);
}
