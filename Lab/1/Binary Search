#include<bits/stdc++.h>
using namespace std;


int main()
{
    int n, arr[100],i, vl;
    scanf("%d %d", &n, &vl);
    for(i=0; i<n; i++)scanf("%d", &arr[i]);
    sort(arr, arr+n);
    if(binary_search(arr,arr+n, vl))puts("found");
    else puts("No");
}
