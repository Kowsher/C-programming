#include<stdio.h>
#define func(x) exp(x)-3*x

int main()
{
    double low=1, up=0, mid, n=100;
    while(n--){
        mid=(up+low)/2.0;
        if(func(mid)>0)up=mid;
        else low=mid;

    }
    printf("%.4lf\n", mid);
}
